server:
  port: 8083
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
management:
  endpoints:
    web:
      exposure:
        include: '*'